import id from '../helpers/uuid';

const userName = `thisIsACoolName${id}`;
const email = `vietnamese${id}@very.nice.com`;
const password = '1SecretP@ssword';
const invalidPass = 'invalidPassword';

export { userName, email, password, invalidPass };
